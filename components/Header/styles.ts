import EStyleSheet from 'react-native-extended-stylesheet';

EStyleSheet.build();
export const headerStyle = EStyleSheet.create({
  container: {
    marginTop: '3rem',
    marginBottom: '1rem',
    marginLeft: '5%',
  },
});
